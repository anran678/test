import signal
import sys

def signal_handler(signum, frame):
    print(f"信号 {signum} 在以下位置触发：")
    traceback.print_stack(frame)
    sys.exit(1)

# 注册浮点异常信号（SIGFPE）
signal.signal(signal.SIGFPE, signal_handler)
